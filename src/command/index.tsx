import React from "react";
import { ReactNode } from "react";
import { EventsComponent } from "../../component/lib/events.component";

export class CommandSelector extends EventsComponent {
    render(): ReactNode {
        return <div className="command-selector">
            
        </div>
    }
}